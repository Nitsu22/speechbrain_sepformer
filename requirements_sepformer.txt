# Requirements to run `recipes/WHAMandWHAMR/separation/train.py` (SepFormer).
#
# Usage (from repo root):
#   pip install -r requirements_sepformer.txt
#
# This installs SpeechBrain from this checkout (editable), plus the extra
# third-party packages needed by the WHAM/WHAMR separation recipe.

-e .

# SpeechBrain runtime deps (mirrors `requirements.txt` but excludes dev/lint tooling)
torch>=2.1.0,<2.9
torchaudio>=2.1.0,<2.9
numpy>=1.17.0
scipy>=1.4.1
soundfile>=0.12.1
hyperpyyaml>=0.0.1
tqdm>=4.42.0
joblib>=0.14.1
packaging
huggingface_hub>=0.8.0
pandas>=1.0.1

# Recipe-specific extra deps (`recipes/WHAMandWHAMR/extra_requirements.txt`)
mir-eval==0.6
pyroomacoustics>=0.7.3

# Used by SpeechBrain internals (imported as `yaml`)
PyYAML
